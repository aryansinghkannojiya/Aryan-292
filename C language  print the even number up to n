#include <stdio.h>

int main() {
    int n, i;

    
    printf("enter the number : ");
    scanf("%d", &n);

    printf("%d \n", n);

     if(i%2 == 0)
    for (i = 2; i <= n; i = i + 2) {
        printf("%d ", i);
    }

    printf("\n");
    return 0;
}
